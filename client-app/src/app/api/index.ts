import * as ip from "./ip";
import * as servers from "./servers";
import * as speedTest from "./speedTest";
import * as dashboard from "./dashboard";
import * as client from "./client";
import * as myISP from "./myISP";

const api = {
  ip,
  servers,
  speedTest,
  dashboard,
  client,
  myISP,
};

export default api;
